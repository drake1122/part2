-- =====================================================
-- 파츠디에스 회원 유형 설정 SQL
-- mb_7: 회원유형 (normal=일반, business=사업자)
-- mb_8: 사업자등록번호
-- mb_9: 업체명
-- mb_10: 담당자명
-- =====================================================
-- 실행: 최초 1회만 실행 (phpMyAdmin 또는 sql_runner.php)
-- =====================================================

SET NAMES utf8mb4;

-- ── 기존 회원 mb_7 기본값 설정 (기존 회원은 일반회원으로) ──
UPDATE `g5_member`
SET `mb_7` = 'normal'
WHERE `mb_7` = '' OR `mb_7` IS NULL;

-- ── 확인 ──
-- SELECT mb_id, mb_name, mb_7 AS mb_type, mb_8 AS biz_no, mb_9 AS biz_name
-- FROM `g5_member`
-- ORDER BY mb_7 DESC, mb_datetime DESC
-- LIMIT 20;

-- =====================================================
-- 완료!
-- 이후 회원가입/수정 시 mb_7~mb_10이 자동으로 처리됩니다.
-- =====================================================
